import React from 'react';

const Header = () => {
    return(
        <div className="header">
            <h1 className="header__logo">Coral Board</h1>
        </div>
    );
};

export default Header;
